@using Piipan.QueryTool.Client.Helpers
@inject AppData InjectedData
@code {
    [Parameter] public AppData AppData { get; set; }

    protected override void OnInitialized()
    {
        PropertyCopier.CopyPropertiesTo(AppData, InjectedData);
    }
}